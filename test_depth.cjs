const fs = require('fs'); const lines = fs.readFileSync('views/pages/business-loan-for-working-capital.ejs', 'utf8').split('\n'); let o=0, c=0, start=0; for(let i=192; i<723; i++) { const line = lines[i]; if(line.includes('class=\"acco-item\"')) { if(start!==0) console.log(start + '-' + (i) + ': open ' + o + ', close ' + c); start=i+1; o=0; c=0; } o += (line.match(/<div\b[^>]*>/g) || []).length; c += (line.match(/<\/div>/g) || []).length; } console.log(start + '-722: open ' + o + ', close ' + c);
