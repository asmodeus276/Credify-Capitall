const fs = require('fs'); const path = require('path'); const dir = 'views/pages'; let count=0; fs.readdirSync(dir).forEach(file => { if(!file.endsWith('.ejs')) return; const text = fs.readFileSync(path.join(dir, file), 'utf8'); if(text.includes('class=\"row doc-wrap\"') && !text.includes('class=\"IIFL_wrap\"')) count++; }); console.log('Files with row doc-wrap but no IIFL_wrap: ' + count);
