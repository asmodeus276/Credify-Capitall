const fs = require('fs'); const lines = fs.readFileSync('views/pages/business-loan-for-working-capital.ejs', 'utf8').split('\n'); let o=0; for(let i=0; i<lines.length; i++) { const line = lines[i]; const open = (line.match(/<div\b[^>]*>/g) || []).length; const close = (line.match(/<\/div>/g) || []).length; o += open - close; if (line.includes('class="acco-item"') || line.includes('class="card tab-pane') || line.includes('id="content"')) { console.log('Line ' + (i+1) + ' depth before: ' + (o - open + close) + ' -> ' + line.trim()); } }
