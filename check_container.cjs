const fs = require('fs'); const path = require('path'); const dir = 'views/pages'; let earlyClose = 0; fs.readdirSync(dir).forEach(file => { if(!file.endsWith('.ejs')) return; let text = fs.readFileSync(path.join(dir, file), 'utf8'); let uncommented = text.replace(/<!--[\s\S]*?-->/g, match => ' '.repeat(match.length)); let unLines = uncommented.split('\n'); let o = 0; let feedbackFormLine = -1; for(let i=0; i<unLines.length; i++) { if(unLines[i].includes('id=\"feedback-form\"')) { feedbackFormLine = i; break; } } if(feedbackFormLine === -1) return; let zeroLine = -1; for(let i=0; i<feedbackFormLine; i++) { const line = unLines[i]; let open = (line.match(/<div\b[^>]*>/g) || []).length; let close = (line.match(/<\/div>/g) || []).length; o += open - close; if(o === 0 && open - close !== 0 && i > 10) { zeroLine = i; break; } } if(zeroLine !== -1) { console.log(file + ' container closes early at line ' + (zeroLine+1) + ' (feedback is at ' + (feedbackFormLine+1) + ')'); earlyClose++; } }); console.log('Total early close: ' + earlyClose);
