const fs = require('fs'); const file = 'views/pages/business-loan-for-working-capital.ejs'; let text = fs.readFileSync(file, 'utf8'); let uncommented = text.replace(/<!--[\s\S]*?-->/g, match => ' '.repeat(match.length)); let o=0; let drops = []; for(let i=0; i<uncommented.length; i++) { if(uncommented.substr(i, 4) === '<div') o++; else if(uncommented.substr(i, 6) === '</div>') { o--; if(o < 0) drops.push(i); } } console.log('Drops below 0 at indices:', drops.slice(0, 5));
