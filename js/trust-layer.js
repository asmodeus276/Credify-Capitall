export const initTrustBar = () => {
    const trustSection = document.getElementById('trust-layer');
    if (!trustSection) return;

    // Optional: Add entrance animation if needed
    trustSection.classList.add('animate-fade-in-up');
};
